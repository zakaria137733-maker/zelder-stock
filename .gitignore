# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Python model/checkpoints
*.pt
# Deployed ML artifacts are intentionally committed so the container ships with a
# working model: the single fallback checkpoint plus the 5-seed ensemble
# (lstm_model_<seed>.pt) that the predictor actually serves.
!backend/models/lstm_model.pt
!backend/models/lstm_model_*.pt

# Environment
.env
.venv/
venv/

# Node
node_modules/
.next/

# IDE
.vscode/
.idea/