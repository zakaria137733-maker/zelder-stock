NEWS_API_KEY=
USE_FINBERT=false

MONGO_URI=mongodb://localhost:27017/sentimentiq
INFLUX_URL=http://localhost:8086
INFLUX_TOKEN=
INFLUX_ORG=sentimentiq
INFLUX_BUCKET=sentiment_scores
INFLUX_TRADES_BUCKET=stock_trades
REDIS_URL=redis://localhost:6379

# Authentication rate limiting (per scope+identity)
AUTH_RATE_LIMIT=20
AUTH_RATE_WINDOW_SECONDS=300

# Temporal (workflows / scheduled activities)
TEMPORAL_URL=localhost:7233

# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_SECRET=
# Admin login credentials (POST /api/admin/login)
ADMIN_USERNAME=
ADMIN_PASSWORD=
